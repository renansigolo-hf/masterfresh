name: Call multiple reusable workflows

on:
  pull_request:
    branches:
      - main

jobs:
  call_workflow_a:
    uses: ./.github/actions/workflow-A.yml

  call_workflow_b:
    uses: ./.github/actions/workflow-B.yml
