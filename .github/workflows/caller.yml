name: Call a reusable workflow

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  call-workflow:
    uses: renansigolo-hf/masterfresh/pull/1/.github/workflows/workflow-A.yml@v1

  call-workflow-passing-data:
    uses: ./.github/workflows/workflow-B.yml@main
