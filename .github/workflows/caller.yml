name: Call multiple reusable workflows

on:
  pull_request:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."

      - uses: actions/checkout@v2

      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."

  call-workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          path: .github/workflows/workflow-A.yml@v1
      # - uses: renansigolo-hf/masterfresh/.github/workflows/workflow-A.yml@v1

  # call_workflow_a:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: ./.github/workflows/workflow-A.yml

  # call_workflow_b:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: ${{ github.repository }}/.github/workflows/workflow-B.yml
# jobs:
#   call-workflow:
#     uses: octo-org/example-repo/.github/workflows/workflow-A.yml@v1

#   call-workflow-passing-data:
#     uses: octo-org/example-repo/.github/workflows/workflow-B.yml@main
#     with:
#       username: mona
#     secrets:
#       token: ${{ secrets.TOKEN }}
