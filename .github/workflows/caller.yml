name: Call a reusable workflow

on:
  pull_request:
    branches:
      - main

jobs:
  call-workflow:
    uses: renansigolo-hf/masterfresh/pull/1/.github/workflows/workflow-A.yml@v1

  call-workflow-passing-data:
    uses: renansigolo-hf/masterfresh/pull/1/.github/workflows/workflow-B.yml@main
